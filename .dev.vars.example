# Copie para .dev.vars (não versionado) e preencha para rodar `npm run dev`.

# Token compartilhado que autentica os apps/devs chamadores (header Authorization: Bearer ...).
SERVICE_TOKEN=gere_um_token_forte_aqui
